name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  installer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate installer
        run: |
          bash -n install.sh
          shellcheck install.sh tests/installer.sh
          test "$(./install.sh --print-target)" = "x86_64-unknown-linux-gnu"
          ./install.sh --help >/dev/null
          bash tests/installer.sh

  check:
    strategy:
      matrix:
        # The platform guard in src/lib.rs supports exactly these two.
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy, rust-analyzer
      - uses: Swatinem/rust-cache@v2
      - name: Install clangd
        if: runner.os == 'Linux'
        run: command -v clangd >/dev/null || { sudo apt-get update && sudo apt-get install -y clangd; }
      - name: Format
        run: cargo fmt --check
      - name: Clippy
        run: cargo clippy --all-targets --locked -- -D warnings
      - name: Test
        run: cargo test --all-targets --locked

  # Declared rust-version must actually build; `cargo check` without
  # --all-targets on purpose (dev-dependencies need not honor the MSRV).
  msrv:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.100
      - uses: Swatinem/rust-cache@v2
      - name: Check MSRV
        run: cargo check --locked

  deny:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check advisories licenses bans sources
